let varOne = 'LaunchCode';
let varTwo = [1, 5, 'LC101', 'blue', 42];

// 2.a question
for (i = 0; i < varTwo.length; i++) {
    console.log(varTwo[i]);
}

// 2.b question
for (i = varOne.length - 1; i >= 0; i--) {
    console.log(varOne[i]);
}